# Implementation Plans

Generated by the improve skill on 2026-09-14 from the request "improve the
toggle on/off functionality in `install`: it isn't obvious when a skill has
been toggled, and the red line to the left is visually unappealing." Plan 002
was added because the audit of `install` turned up a silent data-loss bug.

Execute in the order below. Each executor: read the plan fully before
starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title                                                      | Priority | Effort | Depends on | Status                    |
| ---- | ---------------------------------------------------------- | -------- | ------ | ---------- | ------------------------- |
| 001  | Make it obvious which skills are on, off, and just changed | P1       | S      | —          | DONE (applied 2026-09-14) |
| 002  | Make the python3 JSON fallback actually write Overrides    | P1       | S      | —          | DONE (applied 2026-09-14) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 001 and 002 are independent but both edit `install`. Run them one after
  the other, not in parallel worktrees, to avoid a merge.
- 002's line numbers assume 001 has not landed. If 001 is done first, match
  002's excerpt on content; the offset is about 10 lines.

## Execution log

- 2026-09-14, plan 001: executor stopped once on a real obstacle (bash 3.2 cannot run `case` inside `$(...)`), fixed via a `state_tag` helper. Second revision moved the tag before the description so it is not truncated. Plan text updated to match. Apply with `patch -p0 < plans/001-make-skill-toggle-state-obvious.patch` from the repo root, then run `./install` once to eyeball it.

## Findings considered and rejected

- `shellcheck` SC2221/SC2222 at `install:211-213` (the `""` case pattern is
  listed twice in `pick_with_menu`): cosmetic, dead pattern, no behaviour
  change. Not worth a plan.
- Making the fzf picker cycle all four States (on/off/name-only/
  user-invocable-only) instead of on/off: would change the key bindings
  documented in README. Deferred; `--set` covers it.
